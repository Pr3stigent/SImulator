import { Players } from "@rbxts/services"

import Fusion, { Computed, Value, Tween, New, OnEvent } from "@rbxts/fusion"
/*
function instructions() {
	return New("TextLabel")({
		Text: "Hi",
	});
}

function button() {
	const state = Value(0);
	const computed = Computed(() => tostring(state.get()));

	return New("TextButton")({
		Size: UDim2.fromOffset(100, 30),
		Text: computed,
		[OnEvent("MouseButton1Click")]: {
			MouseButton1Click: () => {
				print("L");
			},
		},
	});
}

function main() {
	return New("ScreenGui")({
		Parent: Players.LocalPlayer,
	});
}*/

export = {}
